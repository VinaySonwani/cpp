
In case of a global and local variable having the same name then in the block where local var is defined, the local overrides the global.

If you want to use the global value over the local in the same block use, ::global.